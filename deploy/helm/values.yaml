replicaCount: 1

image:
  repository: zerohackorg/shadowred
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9595

resources:
  limits:
    cpu: 500m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi